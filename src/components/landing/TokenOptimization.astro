<!-- Token Optimization Section -->
<section class="section token-optimization section--alt" id="token-optimization">
    <div class="container">
        <h2 class="section-title">Save 60-90% on Tokens</h2>
        <p class="section-subtitle">RTK filters command outputs before they reach Claude's context — 446 stars, 700+ Reddit upvotes</p>

        <div class="rtk-demo">
            <!-- Before/After comparison -->
            <div class="rtk-comparison">
                <div class="rtk-before">
                    <h4>Without RTK</h4>
                    <code>git log &rarr; ~14,000 chars</code>
                </div>
                <div class="rtk-arrow">&rarr;</div>
                <div class="rtk-after">
                    <h4>With RTK</h4>
                    <code>rtk git log &rarr; ~1,100 chars</code>
                    <span class="savings-badge">92% saved</span>
                </div>
            </div>

            <!-- Key commands grid -->
            <div class="rtk-commands">
                <div class="rtk-cmd"><code>rtk git status</code> <span>76%</span></div>
                <div class="rtk-cmd"><code>rtk cargo test</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk vitest run</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk python pytest</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk go test</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk gain</code> <span>analytics</span></div>
            </div>

            <!-- Install CTA -->
            <div class="rtk-cta">
                <code>brew install rtk-ai/tap/rtk</code>
                <div style="margin-top: 0.75rem; font-size: 0.9rem; opacity: 0.7;">
                    Alternative: <code style="background: rgba(255,255,255,0.1); padding: 0.2rem 0.4rem; border-radius: 0.25rem;">cargo install rtk</code> or <code style="background: rgba(255,255,255,0.1); padding: 0.2rem 0.4rem; border-radius: 0.25rem;">curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/main/install.sh | sh</code>
                </div>
                <div style="display: flex; gap: 1rem; justify-content: center; margin-top: 1rem;">
                    <a href="https://www.rtk-ai.app/" class="btn btn-outline" target="_blank" rel="noopener">Visit RTK &rarr;</a>
                </div>
            </div>
        </div>
    </div>
</section>
