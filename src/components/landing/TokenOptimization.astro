<!-- Token Optimization Section -->
<section class="section token-optimization section--alt" id="token-optimization">
    <div class="container">
        <h2 class="section-title">Save 60-90% on Tokens</h2>
        <p class="section-subtitle">RTK filters command outputs before they reach Claude's context — 446 stars, 700+ Reddit upvotes</p>

        <div class="rtk-demo">
            <!-- Before/After comparison -->
            <div class="rtk-comparison">
                <div class="rtk-before">
                    <h4>Without RTK</h4>
                    <code>git log &rarr; ~14,000 chars</code>
                </div>
                <div class="rtk-arrow">&rarr;</div>
                <div class="rtk-after">
                    <h4>With RTK</h4>
                    <code>rtk git log &rarr; ~1,100 chars</code>
                    <span class="savings-badge">92% saved</span>
                </div>
            </div>

            <!-- Key commands grid -->
            <div class="rtk-commands">
                <div class="rtk-cmd"><code>rtk git status</code> <span>76%</span></div>
                <div class="rtk-cmd"><code>rtk cargo test</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk vitest run</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk python pytest</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk go test</code> <span>90%</span></div>
                <div class="rtk-cmd"><code>rtk gain</code> <span>analytics</span></div>
            </div>

            <!-- Install CTA -->
            <div class="rtk-cta">
                <code>brew install rtk-ai/tap/rtk</code>
                <div style="margin-top: 0.75rem; font-size: 0.9rem; opacity: 0.7;">
                    Alternative: <code style="background: rgba(255,255,255,0.1); padding: 0.2rem 0.4rem; border-radius: 0.25rem;">cargo install rtk</code> or <code style="background: rgba(255,255,255,0.1); padding: 0.2rem 0.4rem; border-radius: 0.25rem;">curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/main/install.sh | sh</code>
                </div>
                <div style="display: flex; gap: 1rem; justify-content: center; margin-top: 1rem;">
                    <a href="https://www.rtk-ai.app/" class="btn btn-outline" target="_blank" rel="noopener">Visit RTK &rarr;</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.rtk-demo { max-width: 900px; margin: 0 auto; }
.rtk-comparison { display: flex; align-items: center; justify-content: center; gap: 2rem; margin: 2rem 0; }
.rtk-before, .rtk-after {
    background: var(--bg-tertiary);
    padding: 1.5rem;
    border-radius: 8px;
    text-align: center;
    flex: 1;
    max-width: 300px;
}
.rtk-before h4, .rtk-after h4 {
    margin: 0 0 0.75rem 0;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-secondary);
}
.rtk-before code, .rtk-after code {
    display: block;
    background: var(--surface-elevated);
    padding: 0.75rem;
    border-radius: 4px;
    font-size: 0.875rem;
    color: var(--text-primary);
}
.rtk-after { border: 2px solid var(--accent); background: var(--surface-elevated); }
.rtk-arrow { font-size: 2rem; color: var(--accent); font-weight: bold; }
.savings-badge {
    display: inline-block;
    background: var(--accent);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
    font-weight: 600;
    font-size: 0.875rem;
    margin-top: 0.75rem;
}
.rtk-commands {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
    margin: 2rem 0;
}
.rtk-cmd {
    background: var(--bg-tertiary);
    padding: 1rem 1.25rem;
    border-radius: 6px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.rtk-cmd:hover { transform: translateY(-2px); box-shadow: var(--shadow-md); }
.rtk-cmd code { font-family: var(--font-mono); font-size: 0.875rem; color: var(--text-primary); }
.rtk-cmd span { color: var(--accent); font-weight: 700; font-size: 0.875rem; }
.rtk-cta { text-align: center; margin-top: 3rem; padding: 2rem; background: var(--bg-tertiary); border-radius: 8px; }
.rtk-cta code {
    display: inline-block;
    background: var(--surface-elevated);
    padding: 0.75rem 1.5rem;
    border-radius: 4px;
    font-size: 1rem;
    margin-bottom: 1rem;
}
@media (max-width: 768px) {
    .rtk-comparison { flex-direction: column; gap: 1rem; }
    .rtk-arrow { transform: rotate(90deg); }
    .rtk-before, .rtk-after { max-width: 100%; }
    .rtk-commands { grid-template-columns: 1fr; }
}
</style>
