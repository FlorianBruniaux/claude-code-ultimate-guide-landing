<!-- Why This Guide Section -->
<section class="section why-guide">
    <div class="container">
        <h2 class="section-title">Why This Guide?</h2>
        <p class="section-subtitle">Claude Code transforms your terminal into an AI pair programming environment. Stop wasting time on trial and error.</p>

        <!-- Architecture Visual -->
        <div class="architecture-visual">
            <figure>
                <img src="/claude-code-architecture-overview.jpeg"
                     alt="Claude Code Architecture: AI Models (Opus, Sonnet) connect through Claude Code's orchestration layer to your code environment via File Editing, Command Execution, and Repo Navigation"
                     loading="lazy"
                     width="800">
                <figcaption>
                    <strong>Key insight:</strong> Claude Code is NOT a new AI model — it's an orchestration layer connecting Claude to your dev environment.
                    <br><small>Diagram by <a href="https://www.linkedin.com/posts/mohamed-ali-ben-salem-2b777b9a_en-ce-moment-je-vois-passer-des-posts-du-activity-7420592149110362112-eY5a" target="_blank" rel="noopener">Mohamed Ali Ben Salem</a></small>
                </figcaption>
            </figure>
        </div>

        <div class="comparison-cards">
            <div class="comparison-card comparison-without">
                <div class="comparison-header">
                    <span class="comparison-icon" aria-hidden="true">&#128683;</span>
                    <h3>Without the Guide</h3>
                </div>
                <ul class="comparison-list">
                    <li><span class="status-icon bad" aria-hidden="true">&#10007;</span> Generic prompts that miss context</li>
                    <li><span class="status-icon bad" aria-hidden="true">&#10007;</span> Default config leaving performance on the table</li>
                    <li><span class="status-icon bad" aria-hidden="true">&#10007;</span> Debugging by trial and error</li>
                    <li><span class="status-icon bad" aria-hidden="true">&#10007;</span> Scattered tutorials, outdated in weeks</li>
                    <li><span class="status-icon bad" aria-hidden="true">&#10007;</span> No clear learning progression</li>
                </ul>
            </div>

            <div class="comparison-card comparison-with">
                <div class="comparison-header">
                    <span class="comparison-icon" aria-hidden="true">&#9989;</span>
                    <h3>With the Guide</h3>
                </div>
                <ul class="comparison-list">
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> Battle-tested prompt patterns</li>
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> Optimized CLAUDE.md configurations</li>
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> Proven methodologies (TDD, SDD, BDD)</li>
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> Decision frameworks (agents vs skills vs hooks &mdash; when to use what)</li>
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> 19,000+ lines of curated, maintained content</li>
                    <li><span class="status-icon good" aria-hidden="true">&#10003;</span> Clear path from beginner to power user</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<style>
.architecture-visual {
    margin: var(--space-xl) auto var(--space-2xl);
    max-width: 700px;
    text-align: center;
}
.architecture-visual figure { margin: 0; }
.architecture-visual img {
    width: 100%;
    max-width: 700px;
    height: auto;
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-md);
    border: 1px solid var(--border);
}
.architecture-visual figcaption {
    margin-top: var(--space-md);
    color: var(--text-secondary);
    font-size: 0.875rem;
    line-height: 1.6;
}
.architecture-visual figcaption strong { color: var(--text-primary); }
.architecture-visual figcaption a { color: var(--accent); }
.comparison-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-xl);
    max-width: 900px;
    margin: 0 auto;
}
.comparison-card {
    background-color: var(--surface-elevated);
    border: 2px solid var(--border);
    border-radius: var(--border-radius-lg);
    padding: var(--space-xl);
    box-shadow: var(--shadow-sm);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.comparison-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
}
.comparison-without { border-left: 4px solid #f87171; }
.comparison-with {
    border-left: 4px solid #34d399;
    box-shadow: var(--shadow-md);
}
.comparison-header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--border);
}
.comparison-icon { font-size: 1.5rem; }
.comparison-header h3 { margin: 0; font-size: 1.1rem; font-weight: 600; }
.comparison-without .comparison-header h3 { color: #f85149; }
.comparison-with .comparison-header h3 { color: #238636; }
.comparison-list { list-style: none; padding: 0; margin: 0; }
.comparison-list li {
    display: flex;
    align-items: flex-start;
    gap: var(--space-sm);
    padding: var(--space-sm) 0;
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.5;
}
.status-icon { flex-shrink: 0; font-weight: 700; font-size: 0.9rem; }
.status-icon.bad { color: #f85149; }
.status-icon.good { color: #238636; }
@media (max-width: 768px) {
    .comparison-cards { grid-template-columns: 1fr; }
}
</style>
