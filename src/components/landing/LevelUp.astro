<!-- Level Up Section -->
<section class="section level-up section--alt" id="level-up">
    <div class="container">
        <h2 class="section-title">Level Up</h2>
        <p class="section-subtitle">Stop repeating yourself. Automate your patterns.</p>

        <div class="levelup-grid">
            <a href="/examples/#agents" class="levelup-card">
                <div class="levelup-icon">&#129302;</div>
                <div class="levelup-content">
                    <h3>Automate repetition &rarr; Agents & Commands</h3>
                    <p>Same review prompt 10x? Create an agent once, reuse forever. <code>/commit</code>, <code>/pr</code>, <code>/review</code> — your workflow, automated.</p>
                </div>
                <span class="levelup-arrow">&rarr;</span>
            </a>
            <a href="/examples/#skills" class="levelup-card">
                <div class="levelup-icon">&#128218;</div>
                <div class="levelup-content">
                    <h3>Extend knowledge &rarr; Skills</h3>
                    <p>Teach Claude your stack conventions, TDD workflow, or security checklist. Reusable knowledge modules that persist across sessions.</p>
                </div>
                <span class="levelup-arrow">&rarr;</span>
            </a>
            <a href="/examples/#hooks-bash" class="levelup-card">
                <div class="levelup-icon">&#128737;&#65039;</div>
                <div class="levelup-content">
                    <h3>Secure operations &rarr; Hooks</h3>
                    <p>Block dangerous commands, scan for secrets, auto-format code. Event-driven scripts that run before/after Claude actions.</p>
                </div>
                <span class="levelup-arrow">&rarr;</span>
            </a>
            <a href="https://github.com/FlorianBruniaux/claude-code-ultimate-guide/blob/main/guide/ai-ecosystem.md" class="levelup-card" target="_blank" rel="noopener">
                <div class="levelup-icon">&#128279;</div>
                <div class="levelup-content">
                    <h3>Combine tools &rarr; Ecosystem</h3>
                    <p>Chain Claude Code with Perplexity, Gemini, NotebookLM for complete research-to-delivery workflows.</p>
                </div>
                <span class="levelup-arrow">&rarr;</span>
            </a>
        </div>

        <div style="text-align: center; margin-top: var(--space-lg);">
            <a href="/examples/" class="btn btn-secondary">Browse 113 Templates &rarr;</a>
        </div>
    </div>
</section>

<style>
.levelup-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    max-width: 800px;
    margin: 0 auto;
}
.levelup-card {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    padding: var(--space-lg);
    background-color: var(--surface-elevated);
    border: 1px solid var(--border);
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-normal);
}
.levelup-card:hover {
    border-color: var(--accent-secondary);
    transform: translateX(8px);
    box-shadow: var(--shadow-md);
}
.levelup-icon { font-size: 2rem; flex-shrink: 0; }
.levelup-content { flex: 1; }
.levelup-content h3 { font-size: 1rem; margin-bottom: var(--space-xs); color: var(--text-primary); }
.levelup-content p { font-size: 0.875rem; color: var(--text-secondary); line-height: 1.5; margin: 0; }
.levelup-content code {
    background-color: var(--bg-tertiary);
    padding: 0.125rem 0.375rem;
    border-radius: 4px;
    font-size: 0.8rem;
}
.levelup-arrow {
    font-size: 1.25rem;
    color: var(--accent-secondary);
    flex-shrink: 0;
    opacity: 0;
    transform: translateX(-8px);
    transition: all var(--transition-fast);
}
.levelup-card:hover .levelup-arrow {
    opacity: 1;
    transform: translateX(0);
}
@media (max-width: 640px) {
    .levelup-card { flex-direction: column; text-align: center; gap: var(--space-md); }
    .levelup-card:hover { transform: translateY(-4px); }
    .levelup-arrow { display: none; }
}
</style>
