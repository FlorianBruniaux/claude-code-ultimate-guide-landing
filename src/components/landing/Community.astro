<!-- Community Section -->
<section class="section section--compact community" id="community">
    <div class="container">
        <h2 class="section-title">Community & Resources</h2>

        <div class="community-grid">
            <div class="community-card">
                <span class="community-icon" aria-hidden="true">&#128221;</span>
                <h3>Contribute to the Guide</h3>
                <p>The guide repo is open-source. Found an error? Have a better template?</p>
                <a href="https://github.com/FlorianBruniaux/claude-code-ultimate-guide/blob/main/CONTRIBUTING.md" class="link" target="_blank" rel="noopener">Contributing Guide &rarr;</a>
            </div>
            <div class="community-card">
                <span class="community-icon" aria-hidden="true">&#11088;</span>
                <h3>Star on GitHub</h3>
                <p>If this guide saved you time, show your support.</p>
                <a href="https://github.com/FlorianBruniaux/claude-code-ultimate-guide" class="link" target="_blank" rel="noopener">GitHub Repository &rarr;</a>
            </div>
        </div>

    </div>
</section>

<style>
.community-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-lg);
}
.community-card {
    background-color: var(--surface-elevated);
    border: 1px solid var(--border);
    border-radius: var(--border-radius-lg);
    padding: var(--space-lg);
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-normal);
}
.community-card:hover {
    border-color: var(--accent);
    transform: translateY(-2px);
    box-shadow: var(--shadow-card-hover);
}
.community-card.highlight { border-color: var(--accent); grid-column: span 1; }
.community-icon { font-size: 1.4rem; margin-bottom: var(--space-sm); display: block; }
.community-card h3 { font-size: 1.2rem; margin-bottom: var(--space-sm); }
.community-card p { color: var(--text-secondary); font-size: 0.95rem; margin-bottom: var(--space-lg); }
.community-card .link { font-size: 0.95rem; }
@media (max-width: 768px) {
    .community-grid { grid-template-columns: 1fr; }
}
</style>
